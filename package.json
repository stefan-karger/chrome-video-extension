{
  "name": "chrome_video_extension_v2",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Minimal HLS downloader extension with native ffmpeg host",
  "scripts": {
    "dev": "wxt",
    "build": "wxt build",
    "zip": "wxt zip",
    "compile": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.299",
    "typescript": "^5.7.3",
    "wxt": "^0.20.13"
  }
}
